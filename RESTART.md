# また使うときの手引き

来年また同じように使うときの手順。

**いまの状態**（2026-08-11 時点）:

- 羽田の駐車場はすべて停止済み（P2 を予約できたため）。`monitor-fast` は schedule ごと止めてある
- **赤城山キャンプ場（2026-09-19、F1・F3・F4）は稼働中**。`monitor-normal` が30分ごとに確認している

キャンプ場も不要になったら `targets.yml` の `akagi-family` を `enabled: false` にする。
ただし `akagi.yml` の schedule は生きているので、対象が全部無効だと
`有効な対象がありません` で毎回ジョブが失敗する。**羽田側と同じく schedule も止めること**
（→ 下の「全部止めるとき」）。

仕組みそのものの説明は [README.md](README.md) にある。ここには
**「毎回やること」と「一度はまったこと」だけ**を書いてある。

---

## 1. 何が止まっていて、何を動かせばいいか

| ワークフロー | 中身 | いまの状態 |
|---|---|---|
| `monitor-normal`（`akagi.yml`） | 30分ごとに1回確認 | **稼働中**（赤城山のみ） |
| `monitor-fast`（`haneda.yml`） | 起動ごとに55分間・3分おき | **停止中**（schedule をコメントアウト） |

対象は `targets.yml` に6件ぶん残っている。アダプタ（読み取り部分）も消していないので、
**日付を書き換えて `enabled: true` にすれば動く**。

```
  akagi-family          赤城山オートキャンプ場 3家族サイト（F1・F3・F4）  ← 稼働中
× haneda-parking        羽田空港 P2駐車場（P3も設定内にコメントで残してある）
× haneda-p4             羽田空港 P4駐車場（一般・個室）
× haneda-p5             羽田空港 P5駐車場（一般・個室）
× henna-hotel-haneda    変なホテル東京羽田駐車場
× harmony-haneda-west   ハーモニーレジデンス羽田ウエスト駐車場
```

（`python3 monitor.py --list` と同じ並び。`×` が停止中）

### 全部止めるとき

対象を `enabled: false` にするだけでは足りない。**そのワークフローの `schedule:` も
コメントアウトする**こと。対象が空のまま動くと `有効な対象がありません` で `exit 1` になり、
役目を終えた監視から失敗メールが届き続ける（羽田側で実際にそうなりかけた）。
`haneda.yml` に止めた状態の書き方が残っているので、それを真似ればよい。

---

## 2. 再開の手順

### やることは3つだけ

**① `targets.yml` の日付を書き換える**

日付が書いてあるのは `targets.yml` だけ。アダプタ側に日付は埋まっていないので、
ここだけ直せばよい。書き換えるキーは2種類。

| キー | 使っている対象 | 意味 |
|---|---|---|
| `date:` | `akagi-family` / `henna-hotel-haneda` / `harmony-haneda-west` | 1日だけを見る対象 |
| `config.dates:` | `haneda-parking` / `haneda-p4` / `haneda-p5` | 複数日をまとめて見る対象 |

`priority_dates:` を使っている対象があれば、そこも同じ年に直すこと（**直し忘れると、
過ぎた日付を指したまま静かに効かなくなる**）。

**② 使う対象を `enabled: true` に戻す**

**③ fast を使うなら `.github/workflows/haneda.yml` の `schedule:` のコメントを外す**

②だけだと「対象は有効なのに誰も動かさない」状態になる。fast を使わないなら不要。

### 動かす前に一度だけ確認する

```bash
python3 monitor.py --list      # 有効な対象・日付・グループが一覧で出る
```

`×` が付いていないものが動く対象。日付が意図どおりか、ここで目視する。

---

## 3. 本当に動いているかの確かめ方

**「実行が成功した」と「意図どおり動いた」は別**。過去に一度、成功したのに
条件が違っていたことがあるので、次の3点をログで見る。

### ① 通知が届く経路が生きているか

Actions タブ → 対象のワークフロー → Run workflow → `通知テスト` に ✓ → 実行。
スマホに **【テスト】** 付きの通知が来れば、`NTFY_TOPIC` から配信まで通っている。

> テスト通知は「空きが出ました」とは書かず、その時点の実際の判定結果を載せる。
> 本物と見分けがつかない通知を送らないため。

### ② 設定した条件で動いているか

実行ログの冒頭に、その回が使った設定がそのまま出る。

```
通知条件: 羽田空港 P2駐車場 … 同じ枠で 4 件そろったら通知（空きが続く間は 2 時間ごとに再通知）
繰り返し確認: 55分間、180秒おき（対象 1件）
```

**この行を必ず読むこと。** 条件を満たさない回は判定結果に何も出ないので、
`min_available` を変えても、この行が無いと効いているか確かめられない。

### ③ 判定が合っているか

各項目の行に、サイトが返した生の値が `raw=` で出る。

```
  P2 8/22  空きなし  (raw='full')  第2駐車場(P2/第1ターミナル) 8/22
  F1       空きなし  (列=0, icon=fa-xmark)  【F1】3家族サイト 大
```

サイトの表示と突き合わせて、`空きなし` の判定が正しいかを1回だけ見ておく。

### サイトの作りが変わっていたら

**黙って「空きなし」にはならず、`exit 1` でジョブが失敗する**ように作ってある。
ジョブ失敗のメールは「空きが出た」ではなく「**監視が壊れた**」の合図。
その場合はログのエラーを見て、アダプタを直す（→ README の「対象を増やす」）。

---

## 4. 対象を増やす・変える

| やりたいこと | 方法 |
|---|---|
| 同じ予約システムの別施設 | `targets.yml` にブロックを足すだけ。`python3 discover.py --adapter ban489 --facility <施設名> --date <日付>` で貼る内容が出る |
| 未対応のサイト | `python3 capture.py --url <URL> --name <名前>` でHTMLを保存 → `adapters/_template.py` をコピーして `parse()` を書く |
| P3 も見たい | `haneda-parking` の `areas:` にコメントで置いてある P3 の行を外す |
| 通知条件を変えたい | `min_available`（何件そろったら通知）と `priority_dates`（単独でも通知する日）。詳細は README |

新しいアダプタを書いたら、`tests/` に実データのフィクスチャで確認を足しておくと、
翌年サイトが変わったときに何が違うかすぐ分かる。

```bash
python3 tests/test_monitor.py      # 186項目
python3 tests/test_tls_fallback.py #   3項目
bash    tests/test_push_state.sh   #  13項目
```

---

## 5. 一度はまったこと（同じ穴を掘らないために）

### GitHub のスケジュールは時間どおりに動かない

`*/10`（10分ごと）と書いた期間の実測: **108回動くはずが30回**。
間隔の中央値32分、最大144分、**10分±1分で動いた回数は0**。
`*/5` に縮めても意味がない（GitHubが発火しないため）。

そのため fast 側は「起動の粒度」ではなく **1回の起動の中で55分間・3分おきに繰り返す**
方式にしてある。実測12回でのシミュレーション:

| ループ長 | 監視できる割合 | 見ていない時間の合計 | 空白の回数 |
|---|---|---|---|
| 25分 | 51% | 290分 | 10回 |
| 55分 | 71% | 182分 | 4回 |

**1〜2時間まったく起動しない時間帯は、ループ長を変えても埋まらない。**
どうしても埋めたいなら外部から `repository_dispatch` を叩く必要がある（未実装）。

### 待機中の実行はキャンセルされる

`concurrency` は待機中の実行を1つしか保持しない。実行中に2件来ると古い方が消える。
ログで「実行したはずの回が無い」ときはこれ。動作としては正常（同じ確認なので実害なし）。

### state のコミットに `git pull --rebase` を使ってはいけない

`git push || (git pull --rebase && git push)` は、監視対象を変えた直後などに
同じ箇所が衝突して **rebase が途中で止まったまま `exit 1`** になる。
空きが出ていないのに「監視が壊れた」メールが飛ぶ。

state は「最後に確認した結果」だけを持つ生成物なので、履歴を合流させる意味がない。
いまは [`scripts/push-state.sh`](scripts/push-state.sh) が、最新に載せ替えて
今回の観測で上書きする（後勝ち）。`tests/test_push_state.sh` で確認済み。

### サイトごとの癖

| サイト | 癖 | 対応 |
|---|---|---|
| `haneda-p4.jp` | 中間証明書を送ってこない（TLS検証が失敗する） | 証明書のAIAから中間証明書を取得して補完。**検証は緩めていない** |
| `btimes.jp` | 予約可能なのは14日先まで。それ以前は目的日が表に出ない | `allow_out_of_range: true` で「受付不可」として正常終了させる |
| `489ban.net` | 日付ヘッダの月表記が先頭列にしか無い | 先頭列を基準に「何日後か」で列を求める（列番号の決め打ちはしない） |
| 羽田P2/P3 | URLが変わらない。カレンダーはJSON APIをPOSTで叩いている | アダプタが直接APIを呼ぶ |
| ntfy | ヘッダに日本語を入れると文字化けする | JSON形式の publish エンドポイントを使う |

### 空きは数分で消える

P2で **1分間だけ空いてすぐ消える**のを観測した（12:08にあり、12:09には無し）。
駐車場を狙うなら確認間隔を詰める意味は本当にある。キャンプ場は30分で足りる。

---

## 6. 通知が来ないときに見る順番

1. `NTFY_TOPIC` がリポジトリのSecretに登録されているか（未設定なら**起動直後にジョブが失敗する**設計）
2. スマホのntfyアプリが同じトピックを購読しているか → `通知テスト` で確認
3. ワークフローの schedule がコメントアウトされていないか
4. 対象が `enabled: true` か → `python3 monitor.py --list`
5. 日付が過去になっていないか
6. ログの `通知条件:` の行が意図どおりか（条件が厳しすぎて鳴っていないだけかも）
